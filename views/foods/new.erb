<% if @errors %>
  <div class="errors">
	  <h1>THE KOALAS SCATTER. YOU HAVE <%= @errors.count %> <%= @errors.count == 1 ? "ERROR" : "ERRORS" %>.
	  </h1>
	  <ul>
		  <% @errors.each do |error|  %>
		    <li><h2><%= error %></h2></li>
		  <% end %>
	  </ul>
  </div>
<% end %>


<%= erb(:'foods/_form', :locals => { food: Food.new, method: 'post'}) %>
